Постраничная навигация. Контролируемая — позиция хранится снаружи, поэтому сохраняется при возврате из карточки к результатам.

```jsx
<Pagination page={page} pageCount={21} onPage={setPage}
  pageSize={20} onPageSize={setSize} pageSizeOptions={[10,20,50]} />
```
