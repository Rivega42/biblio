# РУКОВОДСТВО по установке и администрированию «Biblio» (черновик / структура)

> Обязательный документ для реестра росПО (эксплуатационная документация / инструкция по установке; при необходимости — руководство администратора). На русском языке. Заготовка-структура — наполнить из фактических инструкций развёртывания (docker-compose). Плейсхолдеры `<ЗАПОЛНИТЬ: …>`.

## 1. Назначение и состав ПО
- Biblio — система автоматизации библиотек (АБИС); серверная часть + веб-клиент.
- Поставка: контейнеризированная сборка (Docker), разворачивается через docker-compose.

## 2. Системные требования
- **Аппаратные:** `<ЗАПОЛНИТЬ: CPU, RAM, диск>`.
- **Программные:** ОС Linux (`<ЗАПОЛНИТЬ перечень доверенных ОС>`), Docker `<версия>`, docker-compose `<версия>`.
- **Сеть:** `<ЗАПОЛНИТЬ: порты, TLS>`.

## 3. Установка (docker-compose)
> Привести фактические шаги из репозитория.
1. Получить дистрибутив/образы: `<ЗАПОЛНИТЬ: источник в РФ>`.
2. Подготовить файл переменных окружения (секреты в env): `<ЗАПОЛНИТЬ: перечень переменных>`.
3. Запуск: `docker compose up -d` `<ЗАПОЛНИТЬ: точная команда/профиль>`.
4. Инициализация БД и применение миграций: `<ЗАПОЛНИТЬ>`.
5. Создание первого арендатора и администратора (провижининг): `<ЗАПОЛНИТЬ: команда/процедура>`.
6. Настройка TLS (reverse-proxy): `<ЗАПОЛНИТЬ>`.
7. Проверка: `/api/health` `<ЗАПОЛНИТЬ: ожидаемый ответ>`.

## 4. Администрирование
- **Управление арендаторами (тенантами):** создание, словари, тарифы/модули.
- **Пользователи и доступ:** роли, гранты (функция × база × уровень).
- **Резервное копирование:** `<ЗАПОЛНИТЬ: процедура бэкапа PostgreSQL>`.
- **Обновление:** `<ЗАПОЛНИТЬ: процедура; подписанные обновления; без принудительного обновления из-за рубежа>`.
- **Мониторинг и логи:** `<ЗАПОЛНИТЬ>`.
- **Синхронизация edge↔облако:** `<ЗАПОЛНИТЬ при наличии узлов>`.

## 5. Эксплуатация (роли пользователей)
- **Каталогизатор:** `<ЗАПОЛНИТЬ ссылку/краткое руководство>`.
- **Сотрудник книговыдачи:** `<…>`.
- **Читатель (веб-портал):** `<…>`.
- **Администратор:** `<…>`.

## 6. Контакты технической поддержки
- `<ЗАПОЛНИТЬ: организация (российское лицо), e-mail, телефон, режим, SLA>` — согласовать с [08_garantijnoe_pismo_podderzhka.md](08_garantijnoe_pismo_podderzhka.md).

---
*Для подачи в реестр достаточно документа, позволяющего эксперту установить/проверить ПО. Заполнить фактическими инструкциями развёртывания.*
