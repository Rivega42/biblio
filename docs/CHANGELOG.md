# CHANGELOG — ход картографии ИРБИС64

Живой журнал проекта. Обновляется по мере работы; коммиты инкрементальные.

## Фаза 1 — Картографический каркас (готово)
- Инвентаризация обоих корней (14 270 файлов), `inventory.csv`, `DOCUMENT_REGISTER.md`, `todo=0`.
- Конвертация всей документации (Word/PP COM), 14 `FINDINGS_*` по областям.
- Механика конфигов + профили 49 БД (`FINDINGS_02`), протокол (80 функций, `FINDINGS_03`).
- Сводка `CAPABILITY_MAP.md`, `OPEN_QUESTIONS.md` (~60 TODO), `README.md`.
- Залито в `github.com/Rivega42/biblio` (main).

## Фаза 2 — Reconstruction-grade (в работе)
Цель: глубина, достаточная для полной пересборки приложений. Структура:
- `reference/databases/DB_<NAME>.md` — полный справочник каждой БД: PAR, **каждая строка FST** (поле/метод/префикс), **все рабочие листы** `.ws` (страницы+поля) и `.wss` (подполя), словарь полей/подполей, каталог форматов PFT и ветвление по 920, справочники MNU/TRE, связи между БД.
- `reference/arms/ARM_<NAME>.md` — полный разбор INI каждого АРМа (секция за секцией) + экраны/режимы/потоки.
- `reference/format/PFT_LANGUAGE.md` — язык форматирования (команды, UNIFOR, условные, повторяющиеся группы).
- `reference/format/FIELD_DICTIONARY.md` — сводный словарь полей/подполей формата (ЭК + комплектование).

### Журнал Фазы 2
- _(старт)_ Каркас `reference/`, `CHANGELOG.md`, помощник `conv_db.ps1` (конвертация конфигов БД в UTF-8).
- **Волна 1 (готово):** 7 reconstruction-grade справочников из реальных конфигов:
  - `format/PFT_LANGUAGE.md` — язык форматирования (44 конструкции, 41 UNIFOR-код, 16 RTF-команд, 20 кодов ошибок).
  - `format/FIELD_DICTIONARY.md` — сводный словарь полей (74 ЭК + 41 комплектование = 115 полей, по 18 блокам).
  - `databases/DB_IBIS.md` — ядро каталога (~95 полей, 90 префиксов, 13 типов РЛ по полю 920, 60+ форматов, импорт/экспорт MARC).
  - `databases/DB_CIRCULATION.md` — RDR/RQST/RDR_ARH/RDRV: **полная модель циркуляции из конфигов** (заказ→выдача→возврат→продление; `RQSTRDR.pft`; статусы `ste.mnu`/`reservstatus.mnu`) — закрыт пробел Фазы 1.
  - `databases/DB_ACQUISITION.md` — CMPL/PODB/POST: модель заявка→заказ→SZ→поступление→КСУ→списание→перенос в ЭК (~114 префиксов, RTF-бланки КСУ).
  - `databases/DB_VUZ.md` — книгообеспеченность (10 типов записи, «связка», расчёт ККО, поле 693).
  - `databases/DB_AUTHORITY.md` — ATHRA/C/S/G/U/B: 4-блочная модель авторитетного контроля, подполе `^3`, autoin-генерация связанных записей.
- **Волна 2 (в основном готово):**
  - `databases/DB_MBA.md` — МБА: дискриминатор 920 (AB/ODER/CENA/ARHIV), цикл оформление→выдача(O1)→продление(O2)→возврат(S1/S2)→списание(H1)→архив, перенаправление M4 (sigl.mnu), статусы из `irbism.ini`.
  - `databases/DB_CLASSIFICATION.md` — RSUDC/RSBBK/HELP/MESH/TEZ/URUB: три модели иерархии (вышестоящий индекс / усечение кода / термин-`U=`+автоввод), навигаторы.
  - `databases/DB_CATALOG_VARIANTS.md` — IMAGE (образы 951/953/952/955), SK (сводный, сигла 902, импорт `impsvk*`), Sk_ekz/new/user (БД-спутники), VKR (научрук 702, антиплагиат 215^W, доступ RIGHT).
  - `databases/DB_SERVICE.md` — RIGHT (шаблоны прав, `right_ft.pft`), LICH (20 префиксов), ZAPR (ИРИ), COUNT/MORPH/WORK/Deposit, LOG* (протоколы).
  - `arms/ARM_*.md` (6 шт.) — посекционный разбор INI всех АРМов (48 секций, ~7046 параметров; креды/IP не переносились).
  - `databases/DB_SPECIAL.md` — GUAR (5-уровневая иерархия FOND/OPIS/DELO/GUAR/LIST, связь полем 488^T), GURF/GuSk (рубрикаторы фондов), Event (План↔Мероприятие, 488), KZD (календарь, поле 309), PAY (оплаты, ключ читатель RI=), IRI (профили), ARCH (тех. архив).
- **Итог reference/ (Фаза 2 завершена):** 18 документов (10 БД + 6 АРМ + 2 формат), ~5800 строк. `CAPABILITY_MAP.md §13a` ссылается на весь корпус; `OPEN_QUESTIONS.md §A0` фиксирует закрытые Фазой 2 вопросы.

## Фаза 3 — Интеграция проектного комплекта `irbis-web`
- Принят и разложен по `docs/` присланный комплект (design/build/recon/reference) согласно `FILES_MANIFEST.md`.
- Репозиторий реорганизован: `docs/{design,build,recon,reference}` + мой глубокий ресерч перенесён в `docs/recon/deep/`.
- Корневые `README.md` (обзор+ссылки), `.gitignore` (секреты/бинарь/данные + `*.ini`/`.env`), `.env.example` (схема служебных кред по типам АРМ).
- [`docs/recon/INTEGRATION_deep-recon.md`](recon/INTEGRATION_deep-recon.md): глубокий ресерч **закрывает Проход А** комплекта (конфиги `DATAI`); комплект даёт числовой `-3338`, live-сервер `:6666`, эталон `BookCabinet`, модель доступа/протокол-маппинг.
- **Дальше (Фаза 4):** углублённый ресерч по протоколу (проводной формат/`BookCabinet`/Проход Б), полям/подполям, оставшимся `TODO(recon)`.

## Фаза 4 — Углублённый ресерч (протокол / поля / валидация / поведение)
- [`reference/format/FIELD_CATALOG.csv`](recon/deep/reference/format/FIELD_CATALOG.csv) + [.md](recon/deep/reference/format/FIELD_CATALOG.md) — **полный машинный каталог полей/подполей** из 1605 `.wss`: **3366 пар (поле×подполе) на 274 полях**, с режимом ввода/валидации на каждое подполе (text/menu/dict/yes-no/tree/authority).
- [`reference/protocol/PROTOCOL_REFERENCE.md`](recon/deep/reference/protocol/PROTOCOL_REFERENCE.md) — исчерпывающий протокол-референс: сигнатуры 80 функций, карта UTF-8/ANSI (8 исключений), разделители `$0D0A`/`$3130`/`$30`, грамматика поиска (6 операторов, 5 приоритетов), знаковое соглашение возврата; gap проводного формата → 12× `TODO(Проход Б)`.
- [`reference/format/FLC_VALIDATION.md`](recon/deep/reference/format/FLC_VALIDATION.md) — **правила валидации полей (ФЛК)**: `dbnflc.pft` (18 проверок записи) + 35 полевых `!NNN.pft` (~70 правил: обязательность, дублетность инв.№/шифра, контроль ISBN/ISSN, справочные проверки) — закрывает «точные правила полей» (BRIEF §6.3).
- [`JIRBIS2_BEHAVIOR.md`](recon/deep/JIRBIS2_BEHAVIOR.md) — поведенческий референс веб-Читателя из мануала J-ИРБИС 2.0: 42 функции (поиск-по-вводу, фасеты, модель прав 951/955/RIGHT, вход через RDR, заказ/очередь, PDF-просмотр); ключевой вывод — стек EOL + нет accessibility → полная веб-замена оправдана.
- **Дальше:** тела модульных PFT (`Pft.tre`), `.gbl`-задания глобальной корректировки; Проход Б на живом `:6666` (числовые коды, проводной формат) при запуске бэкенда.

## Фаза 5 — Reference-данные и логика обработки/вывода
- [`reference/format/CONTROL_VOCABULARIES.csv`](recon/deep/reference/format/CONTROL_VOCABULARIES.csv) + [.md](recon/deep/reference/format/CONTROL_VOCABULARIES.md) — **31 вендор-стандартный словарь, 940 пар код→расшифровка** (статусы экземпляра/брони, причины отказа/выбытия, виды документов, языки, страны, носители, валюты, периоды) — seed-данные для веб-справочников. Уточнён статус экз. `9`=«На бронеполке».
- [`reference/format/GLOBAL_CORRECTION.md`](recon/deep/reference/format/GLOBAL_CORRECTION.md) — язык глобальной корректировки (`IC_gbl`): 22 оператора (ADD/REP/CHA/DEL/DELR/UNDOR/NEWMFN/CORREC/IF/REPEAT/PUTLOG…), формат `.gbl`, **каталог 124 заданий** по БД (автоввод/КСУ/КО/книговыдача/очистка). Закрыл `TODO(Проход Б #07)` протокол-референса.
- [`reference/format/PFT_MODULES.md`](recon/deep/reference/format/PFT_MODULES.md) — каталог **68 PFT-модулей** (`Pft.tre`), цепочки главных форматов (KN/MN/ASP/BRIEF, диспетчер по 920), **табличные формы для `IC_print`** (`.tab`/`.tbg` декларативные колонки) и статформы (`.stf`/`IC_stat`). Ключ: тела модулей — в `Datai\Deposit\`.
- **Итог reference/format/:** язык PFT, словарь полей, полный каталог полей/подполей, ФЛК, словари, глоб.корректировка, PFT-модули, протокол — полный набор для пересборки слоёв данных/поиска/вывода.

## Проход Б — проводной протокол снят с живого сервера
- [`reference/protocol/WIRE_PROTOCOL.md`](recon/deep/reference/protocol/WIRE_PROTOCOL.md) + зонд `tools/irbis-probe/`: модель **одно TCP на запрос**, сессия по `client_id`; кадры запрос(`\n`)/ответ(`\r\n`); команды `A/B/N/O/K/C/G/D/H/L`; **полный CRUD** (поиск/чтение полей+подполей `^`/рендер серверным PFT/запись/удаление); словарь `H` (автодополнение `count#term`), ресурсы `L` (CP1251, `\x1F\x1E`); числовые коды `0/-140/-202/-603/-3337/-3338`. Закрыт gap §7 PROTOCOL_REFERENCE. IBIS: maxmfn 394.

## Фаза 6 — боевой каркас P0 (`irbis-web/`)
- **Слой протокола** `backend/irbis/`: `IrbisClient` (sync, connection-per-request), `parser` (запись→поля/подполя), `SessionManager` (потокобезопасно: 1 `client_id`, lock, reconnect). Запись через `update_record`.
- **Access-набор** `backend/access/`: гранты функция×база×уровень + роли + аудит; `authz` (точная база > `*`, ранги read/write/admin); хранилище sqlite (dev) / PostgreSQL DDL (prod) — [`ADR-004`](build/ADR-004_access-store-sqlite-postgres.md); 17 unit-тестов зелёные.
- **API-ядро** `backend/core.py` (framework-agnostic) + два транспорта: `server.py` (stdlib, любой Python) и `app_aiohttp.py` (aiohttp, Py3.12) — общая логика. Эндпоинты: `health`, `auth/{guest,staff,reader}`, `search`, `terms` (автодополнение), `record`, `render`, `cover` (встроенная картинка 953^B), `order` (guard+audit, RQST — TODO), `me/cabinet` (формуляр RDR), `resource`.
- **Проверено на живом IBIS** (e2e, оба транспорта): auth guest/staff, authz `401/403`, поиск (K=Android→7), автодополнение, чтение 59 полей, обложка JPEG (3980 б), заказ под грантом. Демо-страница читателя на `/` (поиск+автодополнение+карточка+обложка).
- **Дальше:** asyncpg-реализация Access (prod Postgres); боевой `order`→RQST; файлы/полный текст PDF; фронт React/TS по `TZ_ClaudeDesign_UI`.

## Фаза 7 — фронтенд: дизайн-система на живых данных
- **Дизайн-система ИРБИС-Веб** (Claude Design) внесена в репозиторий: `irbis-web/frontend/` (133 файла) — токены, темы (Рабочая/Театр/a11y), React-компоненты (`SearchBar`/`ResultCard`/`StatusBadge`/`PftBlock`/`Pagination`/`DynamicField`/`FileViewer`…), кликабельный прототип на моках, гайдлайны.
- **Живой каталог** `frontend/app/` (`index.html`+`api.js`+`live.jsx`): дизайн-компоненты на **реальных данных IBIS** через P0 API. Реализованы точки подключения прототипа (`runQuery/recordFor/login/confirmOrder`).
- **Backend для фронта:** `/api/search` отдаёт структурированные карточки (заглавие/автор/год/вид/доступность/обложка — разбор полей 200/700/210/900/910); статика `/ui/` (+`/app`-редирект) в обоих транспортах (`static_files.py`); обложка через `?t=<token>` (img без заголовков).
- **Проверено headless-браузером** (Puppeteer) на живом сервере: гостевой вход, поиск `K=Android`→7 с автодополнением, карточки со статусами «Доступен/Выдан», карточка записи с **обложкой из поля 953**, серверный PFT, экземпляры. Скриншоты сняты (результаты + карточка).
- **Сотрудническая часть на живых данных (готово):** переключатель Читатель/Сотрудник; вход сотрудника; **рабочий стол по грантам** (показаны только разрешённые домены — admin видит 5 плиток, Комплектование скрыто без `acq.receipt`); **Каталогизация** на реальном `DynamicField` (тип поля→контрол, подполя, ФЛК), загрузка записи из IBIS в форму, **сохранение в живой сервер** (песочница `WORK`) с аудитом. Backend: `GET /api/worklist/{db}` (профиль РЛ), `POST /api/record/{db}/{mfn}` (создать/обновить, guard `record.write`), `SessionManager.update_record`. Проверено Puppeteer: вход `admin` → стол (5 задач) → каталогизация → создание записи (MFN присвоен, read-back совпал: 920/200/700/210).
- **Дальше:** книговыдача/инвентаризация/BI на живых данных; прод-сборка фронта (self-hosted React вместо CDN, Tailwind по токенам); файлы/полный текст; боевой `order`→RQST; prod-Postgres (asyncpg).

## Фаза 8 — производственный веб-клиент поверх текущего ИРБИС
Цель: довести до развёртываемого боевого клиента (замена J-ИРБИС), до проектирования своего сервера.
- **Настоящая сборка** `irbis-web/frontend/` на **Vite + React + TypeScript** (без CDN/Babel): дизайн-компоненты — чистые ES-модули, токены через CSS-переменные; `src/{main,api,App,Staff}.tsx`. Бэкенд отдаёт собранный `dist/` на `/` (оба транспорта); прежний in-browser прототип — на `/app`.
- **Конфиг-управляемые базы:** `GET /api/databases` читает реестр сервера (`dbnam1.mnu`) — 43 БД, 31 публичная (служебные RDR/RQST/ATHR* отфильтрованы); селектор баз в UI. Список — с сервера, не зашит.
- **Сотрудник в прод-сборке** (`Staff.tsx`): рабочий стол по грантам + каталогизация на реальном `DynamicField` с сохранением в песочницу `WORK`. Проверено в браузере (Puppeteer): librarian→4 плитки, admin→5; рабочий лист рендерится, сохранение работает.
- **Развёртывание:** многоступенчатый `Dockerfile` (node-сборка фронта + python/aiohttp рантайм), `docker-compose.yml` (env-конфиг, том для sqlite-учёток, `host.docker.internal`), `.dockerignore`, обновлённый `.env.example`, раздел «Развёртывание» в README. `docker compose up -d --build` → клиент на :8080.
- **Расширенный поиск (готово):** режим «Простой/Расширенный» — строки поле+значение, объединение И/ИЛИ → композиция ИРБИС-запроса (`"A=…$" * "T=…$"`) → `searchExpr`. Проверено: Автор=Колисниченко И Заглавие=Android → 2 записи. Заодно починены баги порядка аргументов в пагинации/чипах рубрик.
- **Личный кабинет читателя (готово):** вход по билету (RDR `RI=`, ~173 читателя на сервере) → формуляр (ФИО + книги на руках из поля 40). `/api/me/cabinet` подключён; ПДн не коммитятся (вывод зондов gitignored). Проверено: билет найден (MFN 120), формуляр загружен.
- **Остаточный backlog клиента:** просмотрщик PDF/полного текста (нужен бинарный/fulltext-механизм сверх базового протокола); per-DB поисковые префиксы из `[SEARCH]`; боевой `order`→запись RQST + резерв ячейки; Access на Postgres (asyncpg). — это enhancements, ядро клиента (поиск/карточка/заказ/ЛК/каталогизация/деплой) работает на живом сервере.

## Фаза 9 — собственный сервер (PoC) + объединение демо и продукта
Ключ объединения: **один API-контракт, два бэкенда** ([API_CONTRACT.md](build/API_CONTRACT.md)).
- [`API_CONTRACT.md`](build/API_CONTRACT.md) — формализован шов: эндпоинты/формы данных, которые соблюдают обе реализации.
- [`OWN_SERVER_ARCHITECTURE.md`](design/OWN_SERVER_ARCHITECTURE.md) — архитектура своего сервера: модель (поля/подполя JSONB + материализованные колонки + FTS, нормализованные экземпляры под **ячейки**), поиск (PG tsvector+trgm), миграция из ИРБИС через адаптер, переход strangler-паттерном.
- **`irbis-web/server-own/`** — запускаемый PoC своего сервера: `store.py` (sqlite+FTS5), `migrate.py` (ИРБИС→own.db через адаптер Прохода Б), `app.py` (тот же контракт + отдаёт `frontend/dist`).
- **Проверено:** мигрировано **390 записей IBIS**; **тот же веб-клиент** работает на нашем сервере (`own-0.1`): `K=Android`=7, расширенный (Колисниченко∧Android)=2, карточки/обложки — из НАШЕГО стора. То есть демо «как J-ИРБИС» (поверх ИРБИС, `backend/`) и продукт «свой сервер» (`server-own/`) — на одном фронте.
- **Запись/каталогизация на своём сервере:** staff-вход + `/api/worklist` + `POST /api/record` → тот же `DynamicField`-воркшит сохраняет в наш стор (проверено).
- **PostgreSQL (прод-стор):** `pgstore.py` (JSONB + tsvector + bytea), переключение `OWN_STORE_BACKEND=pg`; `docker-compose.pg.yml`. Проверено: 390 записей в PG, тот же фронт, тот же контракт.
- **Ячеистое хранение #39 — иерархическая модель** (наш сервер > ИРБИС): дерево `location` **здание(адрес)→этаж→помещение→стеллаж→полка→ячейка** (число ячеек на полке разное), плюс **постамат выдачи** и **станция книгоприёма** со слотами; экземпляр живёт в листовой локации либо «на руках». Карточка показывает полный адрес; staff-экран «Ячеистое хранение» — drill-down карта занятости. На sqlite и PostgreSQL. Проверено в браузере.
- **План помещения (визуализация расстановки):** к локациям добавлена геометрия (`gx,gy,gw,gh`); seed раскладывает стеллажи рядами с проходами и считает габариты помещения. Staff-экран рисует **top-down SVG-план помещения** со стеллажами (цвет = заполненность, «вход»), клик по стеллажу → его полки и ячейки. На sqlite и PostgreSQL. Проверено в браузере (Зал А: 3 стеллажа, Стеллаж 01 30/38).
- **Дальше:** книговыдача/возврат через постамат+ячейки на своём сервере; редактор плана (drag-drop стеллажей); инкрементальная синхронизация ИРБИС↔наш сервер и сверка; переключение баз; прод-упаковка own-server (Docker).

## Фаза 10 — архитектура продукта (мультиарендный SaaS) + клиент (фасеты) + контракт
Разворот: от «веб-клиент поверх ИРБИС» к **коммерческому мультиарендному продукту** на сотни библиотек.
- [`design/ARCHITECTURE.md`](design/ARCHITECTURE.md) — целевая архитектура v2: **мультиарендный SaaS** (тенант=библиотека, схема-на-тенанта), **облако-ядро + edge-узлы** (работают офлайн, синхронизируются), **микросервисы + подключаемые модули per-tenant**, strangler-миграция; C4-контейнеры, данные, фазы 0–4, ADR, риски. Топология-схема нарисована.
- [`design/GO_TO_MARKET.md`](design/GO_TO_MARKET.md) — политика продаж: сегменты (публичные/школьные/вузовские/ведомственные), тарифы (Базовый/Стандарт/Профи/Сеть), оси цены, cloud vs on-prem(edge), реестр отечественного ПО/закупки 44-223-ФЗ, миграция-онбординг как услуга.
- [`design/BEST_PRACTICES.md`](design/BEST_PRACTICES.md) — ревью против практик мирового уровня (14 областей) + 🔴 «решить сейчас»: модель консистентности, изоляция тенанта, contract-first, наблюдаемость с нуля, edge-OTA, supply-chain.
- [`design/SYNC_DESIGN.md`](design/SYNC_DESIGN.md) (**ADR-005**) — офлайн-синхронизация edge↔облако: **событийный журнал + владение по доменам** (каталог=облако-master → очередь ревью; книговыдача=edge-master, event-sourced), **HLC** вместо wall-clock, политики конфликтов, дельты/идемпотентность/tombstones, хаос-тесты; альтернативы (LWW/CRDT/ElectricSQL/PowerSync) взвешены.
- [`design/ROADMAP.md`](design/ROADMAP.md) — сквозная декомпозиция: **7 инициатив** (milestones I1–I7) → **13 эпиков** → таски → сабтаски; **Definition of Done**-гейты (код+тест+контракт-тест+изоляция+наблюдаемость); сквозные потоки (миграция/наблюдаемость/безопасность); порядок старта.
- [`design/COVERAGE_MATRIX.md`](design/COVERAGE_MATRIX.md) — поблочное покрытие vs ИРБИС (клиент ≈35%, структура БД 50%, движок 40%, адаптер 55%, свой сервер 20%, миграция 25%); где превосходим (UX/a11y, ячейки+план, контракт+2 бэкенда).
- **GitHub-бэклог:** эпики/саб-issue **SA1–SA4** (#112–115: мультиарендность/edge-офлайн/модули-биллинг/GTM) и **BP1–BP4** (#128–131: платформенные практики/безопасность/стандарты/консистентность), +ранние #95–99; **milestones I1–I7** с привязкой эпиков.
- **Клиент — фасетный поиск (#80, готово):** backend `GET /api/facets` (счётчики через дешёвые под-поиски), фронт — левый рейл фасетов (вид документа/язык/автор) на `FilterChip` + refine-чипы (стек, удаление, простой/расширенный). Проверено в браузере (Язык=Английский 7→1, стек `((K=PHP)*V=05)*J=RUS`).
- **Контракт (#116, готово):** [`build/openapi.yaml`](build/openapi.yaml) — **OpenAPI 3.1, 17 эндпоинтов**, валиден (Redocly/swagger-cli, 0 ошибок), стал источником истины; задокументированы расхождения код↔контракт (`/api/storage` только own-сервер; `holdings[]` только own-сервер).
- **Миграция (бенчмарк, готово):** [`design/MIGRATION_BENCHMARK.md`](design/MIGRATION_BENCHMARK.md) + харнесс `server-own/bench_migrate.py`. IBIS=390 записей за **51 с (~7.5 зап/с, sqlite)**; узкое место — **чтение из ИРБИС (78%, 2 TCP round-trip/запись через connection-per-request)**, не запись. Оптимизированно (параллель-8 + батч/COPY, локальный `@brief`) **~50 зап/с → 100k за ~33 мин, 1M за ~5.6 ч**. PG-путь 6.1 зап/с (autocommit-per-row). Вывод для I1: переливать параллельно + батчем + отложенной FTS-индексацией.
- **Диаграммы в репо:** топология + C4-контейнеры ([`design/ARCHITECTURE.md`](design/ARCHITECTURE.md) §2–3) и владение синхронизацией ([`design/SYNC_DESIGN.md`](design/SYNC_DESIGN.md) §1) перенесены из чата как **Mermaid** (рендер на GitHub). Правило — [`CONVENTIONS.md`](CONVENTIONS.md): любая схема живёт в репо, формат по умолчанию Mermaid.
- **Продуктовые механики:** [`design/PRODUCT_PRACTICES.md`](design/PRODUCT_PRACTICES.md) — каталог практик мирового уровня (discovery/вовлечение/логистика/каталогизация/SaaS/ИИ + углублённый **библиотечный домен**: union-каталог/МБА/ERM/периодика/репозиторий/IIIF/BIBFRAME/Z39.50-OAI-SIP2-серверы/readers-advisory…). Топ-6 ★-differentiators. Заведены эпики **PMA–PMF** (#143–148) + 11 саб-issue (#132–142), привязаны к milestones I2/I3/I5/I7.
- **Дизайн-система (ТЗ):** [`design/TZ_CLAUDEDESIGN_design-system.md`](design/TZ_CLAUDEDESIGN_design-system.md) — задание ClaudeDesign на полноформатную систему: токен-фундамент (primitive/semantic/component → CSS-переменные) + **два стиля** (A «Claude-like» приложение light/dark — для продукта/ЛК/инструментов; B Apple Glassmorphism — для сайта проекта) + **мультиарендная кастомизация** (редактор тем, white-label, пресеты по типу библиотеки, contrast-guards AA). Эпик **DS** (#154) + 5 саб (#149–153) → I2.
- **Mermaid-диаграммы починены:** GitHub не рендерил из-за литерального `|` в подписях рёбер — убрано; все 4 диаграммы (топология, C4, владение sync, токены дизайн-системы) проверены mermaid-cli (рендерятся).
- **Детальная проработка I1:** [`design/specs/SPEC_I1_foundation.md`](design/specs/SPEC_I1_foundation.md) — эпики #112/#99/#128 расписаны по таскам/сабтаскам с **критериями приёмки (AC)**, техническим подходом, зависимостями и **DoD**; зафиксирован критический путь I1; ссылка на спеку прикреплена комментариями к эпикам. Начат каталог `design/specs/` (по спеке на инициативу).
- **Учтены 2 упреждающих запроса:** (1) **AI-навигатор + Cmd+K** — ассистент открывает нужный раздел/экран/запись и подсвечивает (deep-link + text-fragment + OCR-координаты); для справочного портала (docs-as-site) и для приложения → задача #162 под PMF #148 (I7). (2) **Конвейер сканирование→DAM→OCR→теги→каталог→IIIF** — заменяет отдельный сканирующий продукт (кейс СПб театральной: книги + изображения/эскизы) → эпик **DG** #161 + 6 саб (#155–160, I5/I2). Отражено в ARCHITECTURE §3 (модули «Сканирование/DAM/оцифровка», «Help-портал/AI-навигатор») и PRODUCT_PRACTICES.
- **Конкурентный ресерч (внешний, 5 сегментов параллельными агентами):** [`design/COMPETITIVE_LANDSCAPE.md`](design/COMPETITIVE_LANDSCAPE.md) + [`design/COMPETITIVE_MATRIX.md`](design/COMPETITIVE_MATRIX.md). Профили РФ (ИРБИС, МАРК Cloud, Руслан-Нео, МегаПро, OPAC-Global, 1С, ЛИБЭР) и мира (Alma/Primo, OCLC WMS, SirsiDynix, Innovative/Vega, FOLIO/Koha/Evergreen, TIND), DAM (DSpace/Islandora/Samvera/Goobi/Preservica/IIIF), вовлечение (BiblioCore/Libby/StoryGraph/смарт-локеры/AI 2024–25). **Матрица по 24 осям.** Подтверждены наши «синие океаны» (edge/офлайн, ячейки+постаматы, маркетплейс, accessibility-first); главные угрозы — МАРК Cloud/Руслан-Нео/МегаПро. **Пробелы → бэклог:** эпики **CN** #181 (консорциумы/union-каталог/СКБР-ЛИБНЕТ-НЭБ/NCIP-SRU, I4) и **ER** #182 (ERM/периодика/резервы, I5) + 10 задач #171–180 (RAG-цитирование/anti-hallucination, appeal-таксономия, BIBFRAME entity-discovery+LOD, кросс-тенант соц-граф, Showcases/browse, платформа вовлечения, SQL-аналитика+бенчмарк, webhooks/мини-приложения, мобильное приложение, DAM-усиления) под PMA/PMB/PME/PMF/DG.
- **Детальная проработка I2:** [`design/specs/SPEC_I2_catalog_search.md`](design/specs/SPEC_I2_catalog_search.md) — эпики #97/#130/#143/#144/#154 расписаны по таскам/сабтаскам с AC/подходом/DoD; интегрированы выводы конкурентного ресерча (семантика+RAG с цитированием/anti-hallucination, appeal-таксономия, FRBR, Showcases/browse, кросс-тенант соц-граф, мобайл, Z39.50/OAI/SIP2 + ЕСИА/НЭБ/КСУ). Критический путь + выходной критерий зафиксированы; ссылка прикреплена к эпикам.
- **Детальная проработка I5 (наполненность замены АРМов):** [`design/specs/SPEC_I5_staff_arm_parity.md`](design/specs/SPEC_I5_staff_arm_parity.md) — по-АРМно (Каталогизатор/Комплектатор/Книговыдача/Книгообеспеченность/Администратор), грунтовано на recon (ARM_*.md, DB_*.md, ФЛК/глоб.корректировка/словари), с AC и выходным критерием «полный рабочий день без десктоп-АРМа». Эпик #98 разбит на **ws1–ws5** (#183–187, →I5), закрывая самые низкие зоны покрытия (Каталогизация 20%, Книговыдача 10%, Комплектование 5%, КО 3%, Админ 25%).
- **Полевая детализация ws1 Каталогизатор:** [`design/specs/SPEC_ws1_cataloger.md`](design/specs/SPEC_ws1_cataloger.md) — РЛ по 920, **ФЛК-движок ~70 правил** (dbnflc 18 + !NNN ~52, семантика 0/1/2, словари jz/str/vd/920.mnu, дублеты I=/B=/IN=, ISBN/ISSN), авторитеты/классификаторы, заимствование Z39.50/СКБР, импорт/экспорт MARC, инвентаризация 910+ячейки, глоб.корректировка (.gbl) с откатом, печать. Полевые AC + выходной критерий. Грунтовано на recon (FLC_VALIDATION/FIELD_CATALOG/GLOBAL_CORRECTION). Прикреплено к #183.
- **Дальше:** реализация **I1** по [`SPEC_I1_foundation.md`](design/specs/SPEC_I1_foundation.md) — PostgreSQL+Access (#99/#92) → изоляция тенантов (#100) → Identity/Tenancy/Licensing (#101); параллельно наблюдаемость (#117) и CI/CD-гейты (#118). Спеки I3/I4/I6/I7 — по мере подхода.
