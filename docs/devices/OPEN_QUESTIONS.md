# OPEN_QUESTIONS — нерешённое по надстройке управления устройствами

> Что осталось неясным, и что нужно (доступы/стенд), чтобы наша Biblio научилась драйвить те же устройства.
> **Обновлено после декомпиляции** — .NET (ilspycmd): §4 (контракт api.svc), §5 (теги карт ЕКП), §6 (поле 691/RQST из боевых FST), §10 (REST) ✅. **Нативной (Ghidra, `EasyBook_RFid.dll`): §8 (ISO 28560) и §9 (EAS/AFI) ✅** — формат стандартный, см. [TAG_DATA_MODEL.md](TAG_DATA_MODEL.md). Остаются открытыми только **железо-зависимые §1–3** (прошивки станций) и низкоприоритетная §7 (схема лицензии).
> Связанные: [DEVICE_INVENTORY.md](DEVICE_INVENTORY.md) · [DEVICE_CONTROL_MAP.md](DEVICE_CONTROL_MAP.md) · [DEVICE_IRBIS_INTEGRATION.md](DEVICE_IRBIS_INTEGRATION.md)

## Шкала важности
🔴 блокер для замены · 🟡 важно · 🟢 уточнение

---

## 1. 🟠 Привод ячеек постаматов / SafeKeeper — station↔server протокол (без перепрошивки)
> Уточнение: рабочие задачи (заказ→ячейка→выдача) выполнимы, если Biblio займёт роль центрального сервиса — **перепрошивка не нужна**. Недостаёт только station-facing половины протокола (что станция шлёт серверу). Подробно — [CONTROL_CAPABILITY.md](CONTROL_CAPABILITY.md) «Рабочие задачи без перепрошивки».
**Факт:** в `LibraryAdminServer.exe` и `IDLogicTagServiceLAS.exe` **нет** команд открытия ячеек/реле (грепом подтверждено отсутствие `OpenCell/CellOpen/OpenDoor/OpenLock/Unlock/solenoid/Relay/Modbus/SerialPort/System.IO.Ports/\\.\COM`). LAS лишь пишет метаданные заказа через `api.svc`; физическое открытие — мастер‑ключом на самой камере (`manual.txt:225,231`).
**Вывод:** реальный контроллер ячейки (релейная плата / RS485‑шина / соленоид) — в **прошивке станции SafeKeeper/постамата**, которой в этой поставке нет.
**Что нужно:**
- доступ к **контроллеру станции SafeKeeper/постамата** (отдельный модуль ПО / прошивка / её дистрибутив `TagServiceLAS`‑аналог на самом киоске);
- либо сетевой трейс «LAS/сервер → станция» при укомплектовании/выдаче;
- либо документация IDlogic на станционный протокол (релейная плата: тип, адресация ячеек, команда открытия).
**Зачем:** без этого Biblio не сможет **физически открыть ячейку** — только вести учёт заказов.

## 2. 🔴 Логика самой станции книговыдачи / киоска (Vending)
**Факт:** станция отдаёт серверу события и здоровье узлов (`IsPrinterOk`, `IsConveyorOk`), но книговыдача/печать чека/конвейер происходят **на киоске**; в LAS команд запуска нет.
**Что нужно:** ПО/прошивка киоска самообслуживания (вероятно отдельный «station»‑build IDlogic), его конфиг, протокол к чековому принтеру и механизму выдачи.
**Зачем:** воспроизвести сам сценарий самообслуживания, а не только статистику.

## 3. 🟡 Счётчик посетителей в воротах (вход/выход)
**Факт:** в `RFID_ANTIVOR_ActiveX.dll` счётчика нет; LAS читает агрегаты `MON_GateCounter` (`CounterIP`/`CounterPort`/направление) с сервера.
**Что нужно:** модель/вендор счётчика (встроен в ворота? отдельный ИК/видео‑датчик?), его сетевой протокол (IP/порт). Возможно FEIG people‑counter или Dahua‑видео.
**Зачем:** реплицировать вход/выходную статистику.

## 4. ✅ РАЗРЕШЕНО (клиентская сторона) — JSON‑контракт `api.svc`
Декомпиляция `WebService.cs` выполнена: **151 операция** (route → тело → ответ) и **106 DTO** извлечены целиком — [CENTRAL_SERVICE_API.md](CENTRAL_SERVICE_API.md). Остаётся неизвестной только **серверная** сторона: коды ошибок/валидаторы и точные тела ответов конкретных операций (тип-контракт известен, наполнение — нет). Для этого по-прежнему полезен один сетевой трейс LAS↔`api.svc`.

## 5. ✅ РАЗРЕШЕНО — Тег подполя RDR для RFID/ЕКП‑карты
**Было неясно**, теперь подтверждено декомпиляцией `EasyBookAbis.Irbis.Irbis`:
- **ЕКП → поле `28`** (`AddEkp`/`AddReader`, `Irbis.cs:286,294,333`);
- **карта/билет → поле `30`** (`AddReaderByCard`, `:381`);
- **доп. карта → поле `24`** (`AddExtraCard`, `:2367-2380`);
- **PIN → поле `103`** (`GetClientName`, `:626`); GUID → `12000`; хэши поиска → `12814/12815`.
Подробно — [EASYBOOKABIS_IRBIS_MAP.md](EASYBOOKABIS_IRBIS_MAP.md) §2,§3. → Для Biblio привязки хранить в этих же полях/эквивалентах.

## 6. ✅ РАЗРЕШЕНО — Поле 691 и заказы RQST
Из боевых FST каталога (`Datai/IBIS/ibis.fst`) расшифровано: **поле 691 = книгообеспеченность** (учебная литература), подполя — `^A`=факультет(FAK), `^C`=специальность(SPEC), `^F`=семестр(SEM), `^O`=форма обучения(FO), `^V`=вид(VO), `^D`=дисциплина(DISC), `^B`=кафедра, `^N`=направление, `^R`=группа(GR), `^i`=IDD, `^L`=филиал. → `GetRecomendedDocs` сопоставляет 691 книги с программой читателя (поле `90`: `^A/^C/^F/^O/^V`) = **рекомендация курсовой литературы** (это НЕ заказы).
**Заказы** — стандартная БД ИРБИС **RQST** (`RI=` индекс, поля 30/903/910), `Hold` через ИРБИС — заглушка; бронь ведётся в Device Service.
**Остаток:** мелкая сверка точных полей записи RQST на дампе — не блокер (стандартная БД).

## 7. 🟢 Бинарные `.cfg/.cfg2/.ekp` и схема лицензирования
**Факт:** `EasyBook_rfid.cfg` (20 б) и `.ekp` (12 б) — бинарные (lightly‑encoded); декод — в `EasyBook_RFid.dll`/`LicenseChecker.dll`. Лицензия привязана к `DeviceID` (GUID) и проверяется веб‑вызовом `DeviceIsLicenseValid` + локально.
**Что нужно (если важно):** декомпиляция `LicenseChecker.dll`/`EkpMed*.dll`.
**Зачем:** понимать привязку лицензий считывателей (для миграции парка). Низкий приоритет — нашей системе своя лицензия.

## 8. ✅ РАЗРЕШЕНО — ISO 28560 / Danish, раскладка байт
Декомпиляция нативного `EasyBook_RFid.dll` (Ghidra) показала: формат **стандартный ISO 28560-2** (без проприетарщины) — precursor-байт (компакция в битах 6-4, relative-OID в 3-0), компакция 1=integer/2=hex/3-5=5-6-7-бит текст/6=octet (подтверждено функциями `FUN_0043c520/c478/c350/c294`). Полная спецификация — [TAG_DATA_MODEL.md](TAG_DATA_MODEL.md).
**Остаток (мелочь):** точное соответствие конкретного relative-OID ↔ поле (штрихкод/owner/set) — закрывается одним дампом реальной метки (`RfidDM15RawRead`). На объём Biblio не влияет: кодек стандартный.

## 9. ✅ РАЗРЕШЕНО (механизм) — AFI/EAS
Декомпиляция подтвердила: команды — стандартные `NXPICODESLIX_EableEAS/DisableEAS/EASCheck` + `ISO15693_WriteAFI` (vtable-диспетч из `RfidEASSet/Get`/`RfidEASAFIPasswordSet`); значения **AFI ON/OFF и пароль EAS конфигурируемы** (`edAFION/edAFIOFF/edEASPass`, дефолт `00000000`) — в DLL не зашиты, реверсить нечего ([TAG_DATA_MODEL.md](TAG_DATA_MODEL.md) §5).
**Остаток:** конкретные значения AFI/пароля у боевого фонда — взять из конфига стенда при миграции.

## 10. ✅ РАЗРЕШЕНО — SOAP vs REST у `api.svc`
Декомпиляция `WebService.cs` подтверждает: клиент использует **REST/JSON** (`HttpClient.PostAsync`, `application/json`, `Newtonsoft.JsonConvert`), Basic‑auth; никакого SOAP/WCF‑биндинга в клиенте нет. → строить как REST.

---

## Что нужно для тестового стенда (сводно)
| Ресурс | Зачем | Приоритет |
|---|---|---|
| Контроллер/прошивка станции SafeKeeper + постамата | привод ячеек, протокол открытия | 🔴 |
| ПО киоска самообслуживания (Vending station build) | сценарий книговыдачи, принтер/конвейер | 🔴 |
| Сетевой трейс LAS↔`api.svc` и LAS↔станции | точные JSON‑контракты и станционный протокол | 🟡 |
| Доступ к боевой БД ИРБИС (RDR/RQST/KNIGA) на стенде | поля карты, 691, RQST, статусы 910^H | 🟡 |
| Образец промаркированной метки (HF ISO15693 + UHF EPC) | побайтовая модель ISO 28560/Danish, AFI/EAS | 🟡 |
| Физические образцы: настольный считыватель, ворота, камера Dahua | проверка нашей реализации «по проводу» | 🟡 |
| ~~Декомпиляция .NET `EasyBookAbis/LibraryAdminServer/TagService`~~ ✅ ВЫПОЛНЕНО (ilspycmd) | теги полей, контракты, DTO — извлечены | ✔ |
| ~~Декомпиляция `EasyBook_RFid.dll` → ISO 28560 (§8), EAS/AFI (§9)~~ ✅ ВЫПОЛНЕНО (Ghidra) | формат стандартный ISO 28560-2 — [TAG_DATA_MODEL.md](TAG_DATA_MODEL.md) | ✔ |
| Декомпиляция `LicenseChecker`/`EkpMed` (схема лицензии §7) — низкий приоритет | внутренняя крипто-схема лицензий | 🟢 |

## Чем НЕ блокированы (реплицируемо уже сейчас)
- Настольный RFID‑считыватель (COM/USB/TCP/PCSC, команды по вендору) — см. [DEVICE_CONTROL_MAP.md](DEVICE_CONTROL_MAP.md) §2,§4.
- Противокражные ворота (COM, EAS_Set/Reset/Alarm, Andea R‑Pan/FEIG) — §5.
- Камеры/FaceDetect (Dahua NetSDK, TCP:37777) — §6.
- Интеграция с ИРБИС (ManagedClient.4 / SIP2), поля RDR/910 — [DEVICE_IRBIS_INTEGRATION.md](DEVICE_IRBIS_INTEGRATION.md).
