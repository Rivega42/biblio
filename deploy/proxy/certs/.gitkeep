# TLS certs live here at runtime (server.crt / server.key) — generated by
# deploy/proxy/gen-cert.sh for dev, or dropped in from your CA for prod.
# The certs themselves are gitignored; only this placeholder is tracked so the
# mount path exists.
